<!DOCTYPE html>
<html>
  <head>
    <title>petspace - for the love of pets</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
  </head>

  <body>

    <div class="navbarformat">
      <nav class="navbar fixed-top navbar-expand-xl navbar-light bg-custom-color">
        <a class="navbar-brand" id="navbarfontsize" href="/"><strong> petspace</strong> </a>
          <% if user_signed_in? %>
            welcome, <%= current_user.username %>
          <% end %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <a class="nav-item nav-link active" class="navbarfontsize2" href="/">|   Home  <span class="sr-only">(current)</span></a>
            <a class="nav-item nav-link" class="navbarfontsize2" <%= link_to "|" " " "Sell", new_product_path %></a>
            <% if user_signed_in? %>
              <a class="nav-item nav-link" class="navbarfontsize2" <%= link_to( '|' ' ' 'Logout', destroy_user_session_path, method: :delete) %></a>
            <% else %>
              <a class="nav-item nav-link" class="navbarfontsize2" <%= link_to( '|' ' ' 'Login/Signup', new_user_session_path)  %></a>
            <% end %>
          </div>
      </nav>
    </div>



    <%= yield %>

  </body>
</html>
