<% if current_user == @order.user %>
  <div class="container" >
    <h2 class="textalign">Thanks for your order!</h2>
    <div class="textalign">
      <p><%= link_to 'Keep Shopping', products_path %></p>
      <h3>Order Details:</h3>
      <p>Product: <%= @order.product.name %></p>
      <p>Product Price: $<%= number_with_precision((@order.product_price||0)/100.0, :precision => 2) %></p>
      <p>Shipping Price: $<%= number_with_precision((@order.shipping_price||0)/100.0, :precision => 2) %></p>
      <hr>
      <p>Total Price: $<%= number_with_precision((@total_price||0)/100.0, :precision => 2) %></p>

      <h3>Shipping Destination</h3>
      <address>
      <p>Street: <%= @order.street %></p>
      <p>Suburb: <%= @order.suburb %></p>
      <p>Postcode: <%= @order.postcode %></p>
      <p>State: <%= @order.state %></p>
      <p>Country: <%= @order.country %></p>
      </address>

      <% else %>

      <h1> This order isn't available</h2>
    </div>
  </div>
<% end %>
